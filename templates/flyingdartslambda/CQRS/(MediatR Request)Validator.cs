using FluentValidation;
using Flyingdarts.Lambdas.Shared;

public class (MediatR Request)Validator : AbstractValidator<(MediatR Request)>
{
    public(MediatR Request)Validator()
    {
        
    }
}